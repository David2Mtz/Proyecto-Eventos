export const environment = {
  production: false,
  // Para desarrollo local con el back en localhost
  apiUrl: 'http://localhost:8080/api'
  
  // Una vez desplegado el back en Render, puedes probar localmente apuntando allá:
  // apiUrl: 'https://tu-backend-en-render.onrender.com/api'
};
